#ifndef OS_KERNEL_MATH_H
#define OS_KERNEL_MATH_H

#define MAX(a, b) ((a) >= (b) ? (a) : (b))
#define MIN(a, b) ((a) <= (b) ? (a) : (b))
#define ARRAY_SIZE(a) (sizeof(a) / sizeof((a)[0]))

#endif // OS_KERNEL_MATH_H
